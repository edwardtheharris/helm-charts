#!/bin/bash

kubectl create ns kube-flannel

kubectl label --overwrite ns kube-flannel pod-security.kubernetes.io/enforce=privileged

kubie ns kube-flannel

helm upgrade --install flannel -f values.yaml --namespace kube-flannel flannel/flannel -f values.yaml
