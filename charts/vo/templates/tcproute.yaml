---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: {{ .Release.Name }}
spec:
  parentRefs:
  - name: {{ .Release.Name }}
    sectionName: valkey
  rules:
  - backendRefs:
    - name: valkey
      port: 6379
